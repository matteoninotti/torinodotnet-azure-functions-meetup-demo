# Azure Functions
local.settings.json
bin/
obj/
.python_packages/
__azurite_db*__.json
__blobstorage__/
__queuestorage__/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# .NET
[Dd]ebug/
[Rr]elease/
*.user

# Go
/functions/go/bin/
# `go build ./...` senza -o lascia il binario col nome del package nella
# cartella corrente: 19 MB che in un repo pubblico non se ne vanno piu'.
/functions/go/resizeworker
# pacchetto prodotto da `func pack`, rigenerabile
/functions/go/*.zip

# Node / frontend
node_modules/

# Bicep / IaC
*.bicepparam.local
.azure/

# Risultati dei run di carico (rigenerabili; quelli che finiscono in slide
# vanno committati esplicitamente in load/results/)
load/output/

# Immagini di test: restano solo in locale e vengono iniettate nel pacchetto
# a deploy-time (D33).
#
# functions/images/ e' la sorgente unica su cui si lavora; le tre cartelle
# per linguaggio sono destinazioni popolate copiando da li' prima del build,
# perche' il pacchetto di deploy e' la sola cartella con host.json (D35).
functions/images/*
!functions/images/README.md
functions/python/images/*
!functions/python/images/README.md
functions/dotnet/images/*
!functions/dotnet/images/README.md
functions/go/images/*
!functions/go/images/README.md

# Candidate scartate o in valutazione: restano locali come le altre
meme-candidates/

# macOS
.DS_Store

# Stato locale dell'harness Claude Code, non del progetto
.claude/

# Documenti degli audit: restano locali per D38 — sono meta-discussione sul
# progetto (cosa e' stato trovato, perche' si e' scelto cosi', giudizi sul
# lavoro fatto), e il repo e' pubblico e permanente. Nel repo va il piano
# operativo; il perche' sta nel decision log, nel vault.
audit/

# Note di lavoro locali
fase7.md
map.txt
# File della presentazione: restano locali
presentazione/
